version: "3.8"

services:
  backend:
    build: ./backend
    container_name: schedula-backend
    ports:
      - "5000:5000"
    environment:
      - PORT=5000

  frontend:
    build: .
    container_name: schedula-frontend
    ports:
      - "5173:4173"
    depends_on:
      - backend